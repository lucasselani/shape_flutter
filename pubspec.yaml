name: shape
description: Crie sua dieta usando seus próprios dados e os dados da tabela TACO.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  use_case:
    path: use_case

  cupertino_icons: ^0.1.3
  google_fonts: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  #pedantic: ^1.9.0

flutter:
  uses-material-design: true
  assets:
    - repository/assets/json/